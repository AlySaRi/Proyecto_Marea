

<style>
  *{
    box-sizing: border-box;
    margin: 0;
    padding: 0;
  }

  body {
    background-color: #b4d7f7;
    color: #333;
    line-height: 1.6;
    padding: 20px;
  }

  h1 {
    text-align: center;
    margin-bottom: 10px;
    color: #176fe2;
  }

  hr {
    margin: 10px auto 30px auto;
    border: none;
    border-top: 2px solid #207fec;
    width: 80%;
  }

  .edit-container {
    max-width: 700px;
    margin: 0 auto;
    background-color: #fff;
    padding: 25px;
    border-radius: 15px;
    box-shadow: 0 8px 20px rgba(0,0,0,0.1);
  }

  label {
    display: block;
    font-weight: bold;
    margin-top: 15px;
    margin-bottom: 5px;
  }

  input[type="text"],
  input[type="file"],
  textarea {
    width: 100%;
    padding: 10px;
    border: 1px solid #aaa;
    border-radius: 8px;
    font-size: 15px;
    margin-bottom: 10px;
  }

  textarea {
    resize: none;
  }

  img {
    display: block;
    margin: 10px auto 20px auto;
    max-width: 100%;
    border-radius: 10px;
    box-shadow: 0 4px 10px rgba(0,0,0,0.1);
  }

  /* Botones */
  button {
    background-color: #4CAF50;
    color: white;
    border: none;
    padding: 10px 18px;
    margin: 10px auto;
    border-radius: 8px;
    font-size: 14px;
    display: block;
    transition: background-color 0.3s, transform 0.2s;
  }

  button:hover {
    background-color: #45a049;
    transform: translateY(-2px);
  }

  /* Botón rojo eliminar */
  .delete-btn {
    background-color: #d9534f;
  }

  .delete-btn:hover {
    background-color: #c9302c;
  }

  /* Botón gris cancelar */
  .cancel-btn {
    background-color: #777;
  }

  .cancel-btn:hover {
    background-color: #666;
  }

  footer {
    text-align: center;
    margin-top: 40px;
    font-size: 14px;
    color: #0f0bdd;
  }

  /* Responsive */
  @media(max-width: 500px) {
    .edit-container {
      padding: 15px;
    }

    button {
      width: 100%;
      margin: 8px 0;
    }
  }

</style>


<section class="edit-container">
  <h1>Editar Post</h1>
  <hr>

  <form action="/posts/{{post._id}}" method="POST" enctype="multipart/form-data">


    <label for="commonName">Nombre común:</label>
    <input type="text" id="commonName" name="commonName" value="{{post.commonName}}" required>

    <label for="species">Especie:</label>
    <input type="text" id="species" name="species" value="{{post.species}}" required>


    <label for="location">Lugar donde se encontró:</label>
    <input type="text" id="location" name="location" value="{{post.location}}" required>

    <label>Descripción:</label>
    <textarea name="description" rows="4">{{post.description}}</textarea>

    <label>Imagen actual:</label><br>
    <img src="{{post.imageUrl}}" alt="Imagen actual" width="300">

    <label>Cambiar imagen (opcional):</label>
    <input type="file" name="image" accept="image/*">

    <button type="submit">Guardar cambios</button>
  </form>

  <form action="/posts/{{post._id}}/delete" method="POST" style="display:inline;" onsubmit="return confirm('¿Eliminar este registro?')">
    <button type="submit">Eliminar</button>
  </form>

  <a href="/posts/{{post._id}}"><button type="button">Cancelar</button></a>
</section>

<footer>© 2025 Mis recuerdos.</footer>