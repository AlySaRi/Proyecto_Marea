

<style>  
  *{
    box-sizing: border-box;
    margin: 0;
    padding: 0;
  }  

    body {
      background-color: #b4d7f7;
      color: #333;
      line-height: 1.6;
      padding: 20px;
    }

    h1 {
      text-align: center;
      margin-bottom: 10px;
      color: #176fe2;
    }

    hr {
      margin: 10px auto 30px auto;
      border: none;
      border-top: 2px solid #207fec;
      width: 80%;
    }

    .project-detail {
      max-width: 700px;
      margin: 0 auto;
      background-color: #fff;
      padding: 25px;
      border-radius: 15px;
      box-shadow: 0 8px 20px rgba(0,0,0,0.1);
    }

    .project-detail img {
      display: block;
      max-width: 100%;
      height: auto;
      margin: 0 auto 20px auto;
      border-radius: 10px;
      box-shadow: 0 4px 10px rgba(0,0,0,0.1);
    }

    .project-detail p {
      margin-bottom: 15px;
      font-size: 16px;
    }

    .project-detail h3 {
      margin-top: 20px;
      margin-bottom: 10px;
      color: #444;
    }

    /* Botones */
    .project-detail button {
      background-color: #4CAF50;
      color: white;
      border: none;
      padding: 10px 18px;
      margin: 5px 5px 5px 0;
      border-radius: 8px;
      cursor: pointer;
      font-size: 14px;
      transition: background-color 0.3s, transform 0.2s;
    }

    .project-detail button:hover {
      background-color: #45a049;
      transform: translateY(-2px);
    }

    .project-detail a button {
      text-decoration: none;
    }

    footer {
      text-align: center;
      margin-top: 40px;
      font-size: 14px;
      color: #0f0bdd;
    }

    /* Responsive */
    @media (max-width: 500px) {
      .project-detail {
        padding: 15px;
      }

      .project-detail h1 {
        font-size: 1.5em;
      }

      .project-detail button {
        width: 100%;
        margin: 5px 0;
      }
    }
  </style>




<h1>{{project.title}}</h1>
<hr>

<section class="project-detail">

  <!-- Imagen grande -->
  {{#if project.imageUrl}}
    <img src="{{project.imageUrl}}" alt="{{project.title}}" width="400" style="border-radius: 10px;">
  {{/if}}

  <br><br>

  <!-- Lugar -->
  {{#if project.location}}
    <p><strong>Lugar donde se encontró:</strong> {{project.location}}</p>
  {{/if}}


  <!-- Descripción -->
  <h3>Descripción</h3>
  <p>
    {{#if project.description}}
      {{project.description}}
    {{else}}
      <i>No hay descripción añadida.</i>
    {{/if}}
  </p>

  <br>

  <!-- Botones -->
  <a href="/gallery">
    <button>Volver</button>
  </a>

  <form action="/projects/{{project.id}}/delete" method="POST" style="display: inline;" onsubmit="return confirm('¿Eliminar este registro?')">
      <button type="submit">Eliminar</button>
    </form>

  
  <a href="/projects/{{project.id}}/edit">
    <button type="button">Editar</button>
  </a>

</section>

<br>

<footer>© 2025 Mis recuerdos.</footer>